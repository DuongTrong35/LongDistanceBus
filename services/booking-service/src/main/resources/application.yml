server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/longdistancebus?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update   # dev: update; prod: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
